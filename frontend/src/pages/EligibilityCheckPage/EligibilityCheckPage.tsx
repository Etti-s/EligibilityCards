import { Card } from '@/components/common/Card';

export function EligibilityCheckPage() {
  return (
    <div>
      <h1>בדיקת זכאות</h1>
      <Card>
        <p style={{ margin: 0, color: 'var(--color-text-muted)' }}>
          מסך בדיקת הזכאות לפקיד יוטמע בשלב מאוחר יותר.
        </p>
      </Card>
    </div>
  );
}
